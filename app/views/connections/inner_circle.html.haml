- title "My Inner Circle"

#content_wrapper
  #full_col.shadow.dark_text
    .col_head
      %h2.float_left
        = "My Inner Circle (#{@connections.count})"
      .right
        = link_to "Dashboard", dashboard_path
      .clear
    .block_quote
      = render 'index_list'