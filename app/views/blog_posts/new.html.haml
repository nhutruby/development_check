- self.respond_to?(:title) && title('New Post')
- @show_title = false
= stylesheet_link_tag 'blog_kit'

#content_wrapper
  #main_col
    .col_head
      %h2.float_left
        New Blog Post
      .clear
    .block_quote.left
      = render :partial => 'form'

#sidebar.justify
  = render "nav_blog"
  = render "nav_admin"
.clear

