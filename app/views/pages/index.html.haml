- title "Pages"
#content_wrapper
  #full_col.shadow.dark_text.float_left
    .col_head
      %h2.float_left
        = "Pages"
      .right
        -#- if can? :update, @pages
        = link_to "Add a New Page", new_page_path
        |
        = link_to "Dashboard", dashboard_path
      .clear
    .block_quote
      = render 'index_list'